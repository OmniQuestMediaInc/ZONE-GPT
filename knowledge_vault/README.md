# Knowledge Vault

## Rules for CSV Data Formatting
- All CSV files must include headers in the first row.
- Date columns should follow ISO 8601 format (YYYY-MM-DD).
- Use UTF-8 encoding for all text data.
- Ensure no duplicated columns or rows exist in the dataset.